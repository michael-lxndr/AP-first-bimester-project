spring:
  application:
    name: FirstBimesterProject

  datasource:
    url: jdbc:mysql://localhost:3307/first_bimester_project?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

