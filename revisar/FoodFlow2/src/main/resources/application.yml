spring:
  application:
    name: foodflow

  datasource:
    url: jdbc:mysql://localhost:3306/db_project?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: AlbertCQ2026
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update    # ← CAMBIADO: no borra nada, solo actualiza
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  sql:
    init:
      mode: never    # ← CAMBIADO: no ejecutamos data.sql automáticamente

server:
  port: 8080

logging:
  level:
    com.foodflow: DEBUG